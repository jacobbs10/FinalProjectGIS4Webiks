# Server Configuration
PORT=5000

# MongoDB Configuration
# Using local MongoDB instance. Replace 'emergencyGisDb' with your desired database name.
MONGODB_URI=mongodb://localhost:27017/emergencyGisDb


# JWT Configuration
# IMPORTANT: Replace this with a long, random, and secret string!
JWT_SECRET=Tel Aviv multi agency emergency response system

{
  "name": "server",
  "version": "1.0.0",
  // ... other properties
  "scripts": {
    "start": "node server.js", // Example: Maybe you have 'start' instead of 'dev'
    "test": "echo \"Error: no test specified\" && exit 1"
    // Look here for the script you need to run
  },
  // ... other properties like dependencies
}

